============
Installation
============

At the command line::

    $ easy_install changepoint

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv changepoint
    $ pip install changepoint
